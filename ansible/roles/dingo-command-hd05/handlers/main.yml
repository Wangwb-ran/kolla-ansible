---
- name: Restart dingo-command-hd05 container
  vars:
    service_name: "dingo-command-hd05"
    service: "{{ dingo_command_hd05_services[service_name] }}"
  become: true
  kolla_container:
    action: "recreate_or_restart_container"
    common_options: "{{ docker_common_options }}"
    name: "{{ service.container_name }}"
    image: "{{ service.image }}"
    volumes: "{{ service.volumes | reject('equalto', '') | list }}"
    dimensions: "{{ service.dimensions }}"
    healthcheck: "{{ service.healthcheck | default(omit) }}"
    privileged: True
    pid_mode: "host"
  when:
    - kolla_action != "config"


